export const metadata = {
  title: "Trade Tools | Kassongo",
  description: "Free duty calculator and HS code lookup tools for global trade",
};

export default function ToolsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
